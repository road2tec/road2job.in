# Uploaded files must never be executed as PHP
<FilesMatch "\.(php|phtml|php\d?|phar)$">
    Require all denied
</FilesMatch>

<IfModule mod_php7.c>
    php_flag engine off
</IfModule>
<IfModule mod_php8.c>
    php_flag engine off
</IfModule>

Options -Indexes -ExecCGI
